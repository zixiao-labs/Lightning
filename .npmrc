# Don't auto-install the optional playwright/playwright-core peers: dev here
# pins playwright-core (devDependency) to the browser build already cached in
# ~/Library/Caches/ms-playwright; an auto-installed latest `playwright` would
# resolve first and demand a browser download.
auto-install-peers=false
